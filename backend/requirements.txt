# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.115.6
uvicorn[standard]>=0.34.0
pydantic>=2.11.5
pydantic-settings==2.7.0
python-multipart==0.0.19
requests==2.32.4
python-dotenv
PyYAML==6.0.2                 # Fixed to satisfy paddlex/docling compatibility

# ── Vector database ───────────────────────────────────────────────────────────
chromadb>=0.6.3               # Upgraded to 0.6.3 for newer tokenizers compatibility

# ── Embedding & Reranking models ──────────────────────────────────────────────
sentence-transformers>=2.7.0

# ── PDF parsing ───────────────────────────────────────────────────────────────
docling>=2.81.0
docling-core
docling-parse>=5.0.0
docling-ibm-models>=3.12.0,<4.0.0
pdfplumber                    # Hybrid Table Parser fallback
PyMuPDF==1.25.1               # Table region cropping (hybrid_table_parser_v3)

# ── HuggingFace / Transformers stack ─────────────────────────────────────────
# Versions pinned to satisfy RT-DETR-v2 (transformers >= 4.48) and Chromadb
transformers>=4.48.2
tokenizers>=0.21.0
huggingface-hub>=0.23,<1.0.0
accelerate>=0.29.0
bitsandbytes>=0.43.0
safetensors>=0.4.0
sentencepiece>=0.1.99

# ── Vision / DePlot / Table Transformer ───────────────────────────────────────
Pillow>=10.4.0
numpy>=1.26.0,<2.1.0          # Satisfies tensorflow, numba, and jax
timm>=0.9.0                   # Table Transformer backbone
rich<14.0.0                   # Satisfies bigframes in Colab
fsspec==2025.3.0              # Satisfies datasets in Colab

# ── LlamaIndex integrations ───────────────────────────────────────────────────
llama-index-core
llama-index-readers-docling
llama-index-vector-stores-chroma
llama-index-workflows

# ── Sub-dependencies ──────────────────────────────────────────────────────────
bcrypt
build
chroma-hnswlib
kubernetes
posthog
pypika
dataclasses-json
deprecated
dirtyjson
filetype
tinytag
typing-inspect
jsonref
latex2mathml
deepsearch-glm
easyocr
marko
pypdfium2
python-docx
python-pptx
jsonlines
banks
lxml
typer>=0.12.5,<0.22.0         # docling 2.82 requires <0.22
pylatexenc
polyfactory>=2.22.2
rapidocr>=3.3,<4.0.0
colorlog
onnxruntime
pybase64                      # Required by chromadb 0.6+
overrides                     # Required by chromadb 0.6+
httpx                         # Required by chromadb 0.6+
packaging                     # Used for version verification
