fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.7.0
chromadb==0.5.21
sentence-transformers==3.3.1
docling==2.11.0
PyMuPDF==1.25.1
python-multipart==0.0.19
Requests==2.32.3

# HuggingFace LLM inference (Google Colab — GPU/CUDA environment)
# torch is NOT listed here — Colab provides a CUDA-enabled build pre-installed.
# When running locally: pip install torch --index-url https://download.pytorch.org/whl/cu121
transformers>=4.40.0    # TextIteratorStreamer requires >= 4.36
accelerate>=0.29.0
bitsandbytes>=0.43.0    # 4-bit NF4 quantisation

# Vision model — vikhyatk/moondream2 (figures & diagrams from datasheets)
Pillow>=10.0.0           # PIL image handling for moondream2 input
einops>=0.7.0            # required by moondream2 remote code
timm>=0.9.0              # vision encoder backbone used by moondream2
