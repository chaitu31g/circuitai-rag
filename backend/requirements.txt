fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.7.0
chromadb==0.5.21
sentence-transformers==3.3.1
docling==2.11.0
PyMuPDF==1.25.1
python-multipart==0.0.19
Requests==2.32.3

# HuggingFace LLM inference (Google Colab — GPU/CUDA environment)
# torch is NOT listed here — Colab provides a CUDA-enabled build pre-installed.
# When running locally: pip install torch --index-url https://download.pytorch.org/whl/cu121
transformers>=4.45.0    # Qwen2-VL support requires >= 4.45; TextIteratorStreamer >= 4.36
accelerate>=0.29.0
bitsandbytes>=0.43.0    # 4-bit NF4 quantisation for the chat LLM (Qwen2.5)
safetensors>=0.4.0      # fast, safe weight loading for Qwen2-VL shards

# Vision model — Qwen/Qwen2-VL-2B-Instruct (figures & diagrams from datasheets)
Pillow>=10.0.0          # PIL image handling for vision model input
