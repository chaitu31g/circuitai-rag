# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.115.6
uvicorn[standard]>=0.34.0
pydantic>=2.11.5
pydantic-settings==2.7.0
python-multipart==0.0.19
requests==2.32.4
python-dotenv

# ── Vector database ───────────────────────────────────────────────────────────
chromadb==0.5.21

# ── Embedding & Reranking models ──────────────────────────────────────────────
sentence-transformers>=2.7.0

# ── PDF parsing ───────────────────────────────────────────────────────────────
docling>=2.81.0
docling-core
docling-parse>=5.0.0
docling-ibm-models>=3.12.0,<4.0.0
pdfplumber                    # Hybrid Table Parser fallback
PyMuPDF==1.25.1               # Table region cropping (hybrid_table_parser_v3)

# ── HuggingFace / Transformers stack ─────────────────────────────────────────
# Key constraint matrix:
#   chromadb 0.5.21   → tokenizers <=0.20.3
#   docling 2.82      → huggingface-hub <1.0, typer <0.22
#   docling-ibm-models → transformers <5.0
#   Table Transformer  → transformers >=4.42 (already satisfied)
transformers>=4.42.0,<5.0.0
tokenizers>=0.13.2,<=0.20.3
huggingface-hub>=0.23,<1.0.0
accelerate>=0.29.0
bitsandbytes>=0.43.0
safetensors>=0.4.0
sentencepiece>=0.1.99

# ── Vision / DePlot / Table Transformer ───────────────────────────────────────
Pillow>=10.4.0
numpy>=2.0                    # Colab 2025+ requires >=2 (jax, cupy, opencv)
timm>=0.9.0                   # Table Transformer backbone

# ── LlamaIndex integrations ───────────────────────────────────────────────────
llama-index-core
llama-index-readers-docling
llama-index-vector-stores-chroma
llama-index-workflows

# ── Sub-dependencies ──────────────────────────────────────────────────────────
bcrypt
build
chroma-hnswlib
kubernetes
posthog
pypika
dataclasses-json
deprecated
dirtyjson
filetype
tinytag
typing-inspect
jsonref
latex2mathml
deepsearch-glm
easyocr
marko
pypdfium2
python-docx
python-pptx
jsonlines
banks
lxml
typer>=0.12.5,<0.22.0         # docling 2.82 requires <0.22
pylatexenc
polyfactory>=2.22.2
rapidocr>=3.3,<4.0.0
colorlog
onnxruntime
