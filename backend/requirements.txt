# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.115.6
uvicorn[standard]>=0.34.0
pydantic>=2.11.5
pydantic-settings==2.7.0
python-multipart==0.0.19
requests==2.32.4
python-dotenv

# ── Vector database ───────────────────────────────────────────────────────────
chromadb==0.5.21

# ── Embedding & Reranking models (bge-m3 / bge-reranker-large) ───────────────
sentence-transformers>=2.7.0

# ── PDF parsing (Docling) ─────────────────────────────────────────────────────
docling>=2.14.0,<2.30.0      # 2.14+ has docling-parse 5.x; <2.30 avoids rapidocr/polyfactory deps
docling-core
docling-parse>=5.0.0          # docling>=2.14 requires this (3.x lacks DecodePageConfig)
docling-ibm-models>=2.0.6,<3.0.0  # layout/table models; <3.0 matches docling<2.30
PyMuPDF==1.25.1
huggingface-hub

# ── HuggingFace / Transformers stack ─────────────────────────────────────────
# Stable versions compatible with Docling and ChromaDB
transformers
tokenizers
accelerate>=0.29.0
bitsandbytes>=0.43.0     
safetensors>=0.4.0       
sentencepiece>=0.1.99    

# ── Vision / DePlot / graph extraction ────────────────────────────────────────
Pillow>=10.4.0               # No upper cap - let Colab use its pre-installed version
numpy>=1.24.0
timm>=0.9.0               

# ── LlamaIndex integrations ───────────────────────────────────────────────────
llama-index-core
llama-index-readers-docling
llama-index-vector-stores-chroma
llama-index-workflows

# ── Sub-dependencies (Force-installed to ensure runtime) ─────────────────────
bcrypt
build
chroma-hnswlib
kubernetes
posthog
pypika
dataclasses-json
deprecated
dirtyjson
filetype
tinytag
typing-inspect
jsonref
latex2mathml
deepsearch-glm
easyocr
marko
pypdfium2
python-docx
python-pptx
jsonlines
banks
lxml
typer
pylatexenc                    # required by docling>=2.14 latex backend
polyfactory>=2.22.2           # required by docling 2.2x+
